Agrega en el editor un `<div>` con la clase `rectangle`. Establece las siguientes propiedades:

* Ancho: 300px
* Altura 100px
* Color de fondo: `#000`

Escribe los estilos dentro de la etiqueta `<style>`
